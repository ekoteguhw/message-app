<%= form_for(@message) do |f| %>
	<div class="field">
		<%= f.label :message %>
		<%= f.text_area :content %>
	</div>
	<div class="actions">
		<%= f.submit "Create", :class => 'button' %>
	</div>
<% end %>