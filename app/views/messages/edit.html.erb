<%= form_with(model: @message) do |f| %>
	<div class="field">
		<%= f.label :message %>
		<%= f.text_area :content %>
	</div>
	<div class="actions">
		<%= f.submit "Update", :class => 'button-edit' %>
	</div>
<% end %>